global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: {{ prometheus_job_name }}
    metrics_path: {{ prometheus_metrics_path }}
    static_configs:
      - targets:
          - {{ prometheus_target }}
